name: Validar Best Match

on:
  push:
    branches:
      - 'validation/best-match'
    paths:
      - 'Wind_forecasts/validation/fetch_best_match.py'
      - '.github/workflows/validate_best_match.yml'

permissions:
  contents: write

jobs:
  validate-model:
    runs-on: ubuntu-latest
    env:
      TZ: America/El_Salvador

    defaults:
      run:
        working-directory: Wind_forecasts

    steps:
      - name: Checkout rama de validación
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          pip install openmeteo-requests requests-cache retry-requests numpy pandas

      - name: Ejecutar modelo de validación
        run: python validation/fetch_best_match.py

      - name: Guardar resultados de validación
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/validation/best_match/

          if git diff --staged --quiet; then
            echo "No hay cambios nuevos para guardar."
          else
            FECHA=$(date +%F)
            git commit -m "Validation best_match $FECHA"
            git push
          fi
